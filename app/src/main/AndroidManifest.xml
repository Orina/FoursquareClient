<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="me.elmira.foursquareclient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".FoursquareClientApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".cities.CitiesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".venues.SearchVenuesActivity"/>

        <activity android:name=".venuedetails.VenueDetailsActivity"/>

    </application>
</manifest>
